#cores = ("amarelo", "azul", "roxo")          # Tupla não permite adicionar mais elementos
#print(cores)                                 # A tupla é estatica se tiver 3 elementos vai sempre ter 3
#print(cores[1])                              # Não é possivel remover elementos e nem substituir
#print(cores[:2])

#lista_cores = ["amarelo", "azul", "roxo"]
#lista_cores[1] = "branco"
#lista_cores.append("vermelho")
#print(lista_cores)

tupla_cores = ("amarelo", "azul", "roxo")

for cor in tupla_cores:
    print(cor)
